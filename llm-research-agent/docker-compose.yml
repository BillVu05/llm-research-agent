services:
  agent:
    build:
      context: ..
      dockerfile: llm-research-agent/Dockerfile
    image: llm-research-agent:latest
    # Reads GEMINI_API_KEY / SERPAPI_API_KEY straight from the repo-root .env
    # (gitignored). See .env.example for the expected keys.
    env_file:
      - ../.env
    command: []

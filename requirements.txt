# Runtime dependencies only. Upper bounds are deliberate: an unpinned
# `langgraph>=1.0` will eventually ship a breaking change and turn a green CI
# red for reasons that have nothing to do with this repo.
google-generativeai>=0.5.0,<0.9
python-dotenv>=1.0.1,<2
google-search-results>=2.4.2,<3
langgraph>=1.0,<2
# Direct now (page fetching), not just a transitive of google-search-results.
requests>=2.32,<3
# The vector store: one array and one matmul. See src/agent/retrieval.py.
numpy>=1.26,<3

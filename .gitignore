# Secrets
.env
*.env
!.env.example

# Personal documents - not for a public repo
cv.md

# Virtualenvs
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Ad-hoc eval runs. The committed reference point is
# eval/baseline.json, which is deliberately not ignored:
# a baseline nobody can diff against is not a baseline.
eval-*.json
!eval/baseline.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
